# build output
dist/
.output/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment
.env
.env.production

# macOS
.DS_Store

# Editor
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea
.project
.classpath
*.launch
.settings/
*.sublime-workspace

# Astro
.astro/

# font build artifacts
public/fonts/raw/

# Codex plugin workspace marker
.codex

# Local-only operator notes (runbooks etc.) — must not reach public repo
docs/local/

# Draft staging — .md drafts here get converted + moved into src/content/posts/
# by the AI; the raw inputs never need to ship to GitHub. README stays tracked.
needtosubmit/*
!needtosubmit/README.md
